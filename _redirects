# 根路径重写到主文档。状态码 200 是重写（地址栏不变），301/308 才是跳转。
#
# 目标必须写成「不带 .html」的形式 —— 写成 …/156a50943028.html 时，
# Pages 会对这个内部目标再套一次自带的「去掉扩展名」重定向，
# 于是 200 重写降级成 308 跳转，访客地址栏当场变成仓库里的深路径。
/    /core/vendor/three/build/chunks/9d717bc0/156a50943028    200
