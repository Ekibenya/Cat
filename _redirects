# 原 vercel.json 的 rewrites。
# 目标写成带 .html 的形式：Pages 会对它再套一次「去掉扩展名」的 308 跳转，
# 于是访客地址栏会变成深路径 —— 不好看，但这是唯一验证过立绘正常的配置。
# 换成不带 .html 的写法虽然地址干净，却会把这条重写升格成全站兜底，
# 把所有 404 变成返回主文档，立绘请求也因此拿到 HTML。
/    /core/vendor/three/build/chunks/9d717bc0/156a50943028.html    200
